{{- if and .Values.ingress.enabled .Values.onlyoffice.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: onlyoffice
  namespace: {{ .Values.namespace }}
  # Wildcard Secret platform-tls покрывает onlyoffice.humanitec.ru.
spec:
  ingressClassName: {{ .Values.ingress.className }}
  tls:
    - hosts:
        - {{ .Values.ingress.onlyofficeHost }}
      secretName: {{ .Values.ingress.onlyofficeTlsSecretName }}
  rules:
    - host: {{ .Values.ingress.onlyofficeHost | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: onlyoffice
                port:
                  number: 80
{{- end }}
