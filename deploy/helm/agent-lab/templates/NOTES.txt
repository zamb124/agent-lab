Чарт agent-lab установлен в namespace {{ .Values.namespace }}.

Проверка состояния:
  kubectl get pods -n {{ .Values.namespace }}
  kubectl get svc,ingress,pvc -n {{ .Values.namespace }}

Логи приложения:
  kubectl logs -n {{ .Values.namespace }} deployment/{{ .Values.applications.frontend.serviceName }} -f --tail=200

Применённый образ: {{ include "agentlab.image" . }}
Master нода:       {{ .Values.masterNodeName }}
GPU нода:          по селектору {{ .Values.gpuNodeSelectorKey }}={{ .Values.gpuNodeSelectorValue }}

Платформа:    https://{{ .Values.ingress.apexHost }}
LiveKit:      https://{{ .Values.ingress.livekitHost }}
OnlyOffice:   https://{{ .Values.ingress.onlyofficeHost }}
Grafana:      https://{{ .Values.ingress.grafanaHost }}

Откат предыдущей ревизии:
  helm rollback agent-lab -n {{ .Values.namespace }}
