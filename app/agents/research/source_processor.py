"""
SourceProcessorAgent - агент для обработки найденных источников.

Оценивает релевантность, фильтрует дубликаты и низкокачественные источники,
извлекает ключевые цитаты.
"""

import logging
from app.agents.react_agent import ReActAgent
from app.tools.session.session_tools import session_set, session_get
from app.tools.search.web_extract import extract_web_content, extract_metadata
from app.tools.analysis.text_analysis import analyze_text, extract_key_points

logger = logging.getLogger(__name__)


class SourceProcessorAgent(ReActAgent):
    """
    Обрабатывает и фильтрует найденные источники.
    
    Этап 3 в pipeline исследования:
    - Получает результаты поиска из store
    - Оценивает релевантность каждого источника (0-10)
    - Фильтрует дубликаты и низкокачественные источники
    - Извлекает ключевые цитаты из топовых источников
    - Сохраняет обработанные источники в store
    
    Переиспользуемый компонент: можно использовать для обработки любых списков источников.
    Настройки через store:
    - min_relevance_score: минимальный балл релевантности (default: 6)
    - extract_content: извлекать ли полный контент (default: False)
    - max_sources: максимальное количество источников для обработки
    """
    
    name = "source_processor"
    title = "Обработчик источников"
    description = "Фильтрует и обрабатывает найденные источники"
    is_public = True
    
    prompt = """Ты обработчик источников для системы исследований.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 КОНТЕКСТ
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

👤 Пользователь: {?user_name|Исследователь}
🎯 Исходный запрос: {?store.original_query|не указан}
📚 Найдено источников: {?store.sources_count|0}

РЕЗУЛЬТАТЫ ПОИСКА:
{?store.search_results|нет данных}

ТВОЯ ЗАДАЧА:
1. Проанализировать найденные источники
2. Оценить релевантность каждого (0-10)
3. Отфильтровать дубликаты и низкокачественные
4. Извлечь ключевые цитаты из топовых источников
5. Сохранить обработанные источники

ДОСТУПНЫЕ ИНСТРУМЕНТЫ:
- session_set(key, value) - сохранить данные
- extract_web_content(url, max_length) - извлечь контент страницы
- extract_metadata(url) - получить метаданные (title, description)
- extract_key_points(text, max_points) - извлечь ключевые моменты
- analyze_text(text, focus) - глубокий анализ текста

КРИТЕРИИ ОЦЕНКИ РЕЛЕВАНТНОСТИ (0-10):
- 9-10: Идеально релевантно, прямой ответ на вопрос, авторитетный источник
- 7-8: Очень релевантно, содержит нужную информацию
- 5-6: Релевантно частично, косвенно связано с темой
- 3-4: Слабо релевантно, минимальная связь
- 0-2: Нерелевантно, не по теме

ПРОЦЕСС РАБОТЫ:
1. Проанализируй источники из {store.search_results}
2. Используй {store.original_query} для оценки релевантности
3. Для каждого источника:
   - Оцени релевантность относительно original_query
   - Проверь на дубликаты (похожие URL или идентичный контент)
   - Для топовых источников (score >= 7) извлеки ключевые цитаты
4. Отфильтруй источники с баллом < 6
5. Структурируй обработанные источники
6. Сохрани через session_set("processed_sources", результат)
7. Сохрани статистику: session_set("sources_stats", "total: X, filtered: Y, final: Z")

ФОРМАТ ОБРАБОТАННЫХ ИСТОЧНИКОВ:
Сохрани в виде структурированного текста:

📊 ОБРАБОТАННЫЕ ИСТОЧНИКИ:

🌟 ВЫСОКАЯ РЕЛЕВАНТНОСТЬ (9-10):
1. [Название источника] (URL: ...)
   Релевантность: 9/10
   Ключевые моменты:
   - [Цитата 1]
   - [Цитата 2]

2. ...

✅ ХОРОШАЯ РЕЛЕВАНТНОСТЬ (7-8):
3. [Название источника] (URL: ...)
   Релевантность: 8/10
   Краткое описание: ...

ПРИМЕЧАНИЕ: Если результатов поиска много (>10), обработай топ-10 наиболее релевантных.

ВАЖНО:
- НЕ извлекай контент для всех источников (только для топ-3)
- Дубликаты определяй по домену и похожему title
- Если источник на нерелевантном языке - понизь балл
- После обработки ОБЯЗАТЕЛЬНО сохрани результаты
- Сообщи пользователю статистику обработки"""
    
    tools = [
        session_set,
        extract_web_content,
        extract_metadata,
        extract_key_points,
        analyze_text
    ]
    
    # Для переиспользуемости через store:
    # store = {
    #     "min_relevance_score": 6,
    #     "extract_content": False,
    #     "max_sources": 10
    # }

