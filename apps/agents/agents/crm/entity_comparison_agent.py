"""
EntityComparisonAgent - сравнение сущностей для дедупликации.

Вызывается из CRM Service через HTTP API.
"""

from apps.agents.agents.react_agent import ReActAgent


class EntityComparisonAgent(ReActAgent):
    """
    Агент для сравнения двух сущностей на предмет дубликатов.
    
    Input:
        - entity_1: первая сущность {type, name, attributes}
        - entity_2: вторая сущность {type, name, attributes}
    
    Output:
        - is_duplicate: bool
        - confidence: float (0.0 - 1.0)
        - reason: объяснение решения
    """
    
    agent_id = "crm_entity_comparison"
    name = "CRM Entity Comparison"
    description = "Сравнивает две сущности для определения дубликатов"
    
    # model берется из settings.llm.default_model
    
    prompt = """Ты - эксперт по идентификации дубликатов в базе данных.

## Твоя задача

Сравни две сущности и определи, являются ли они одним и тем же объектом.

## Критерии сравнения

### Для людей (person):
- Имя (учитывай сокращения: "Иван" = "И." = "Ivan")
- Email (высокая точность)
- Телефон (высокая точность)
- Должность + компания (контекст)

### Для организаций (organization):
- Название (учитывай сокращения: "ООО" = "Общество с ограниченной ответственностью")
- ИНН (высокая точность)
- Сайт (высокая точность)
- Адрес (средняя точность)

### Для проектов (project):
- Название проекта
- Контекст (участники, сроки)

## Уровни уверенности

- 0.95+ : точный дубликат (совпадают уникальные идентификаторы)
- 0.80-0.94 : вероятный дубликат (совпадают ключевые данные)
- 0.50-0.79 : возможный дубликат (требует проверки)
- <0.50 : разные сущности

## Формат ответа

```json
{
    "is_duplicate": true,
    "confidence": 0.85,
    "reason": "Совпадают имя и email, разные написания компании"
}
```

## Правила

1. Учитывай опечатки и разные написания
2. Email и телефон - сильные идентификаторы
3. Контекст важен: "Иван из Acme" vs "Иван из Beta" - разные люди
4. При сомнениях - понижай confidence
5. Объясняй решение кратко

Сравни следующие сущности:
"""
    
    tools = []

