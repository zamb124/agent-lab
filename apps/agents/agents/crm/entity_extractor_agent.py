"""
EntityExtractorAgent - извлечение сущностей из текста.

Вызывается из CRM Service через HTTP API.
"""

from apps.agents.agents.react_agent import ReActAgent


class EntityExtractorAgent(ReActAgent):
    """
    Агент для извлечения сущностей и связей из текста.
    
    Input:
        - text: текст заметки для анализа
        - entity_types: список типов с промптами (опционально)
        - generate_summary: генерировать резюме (bool)
    
    Output:
        - entities: список извлеченных сущностей
        - relationships: список связей между сущностями
        - summary: резюме текста (если запрошено)
    """
    
    agent_id = "crm_entity_extractor"
    name = "CRM Entity Extractor"
    description = "Извлекает сущности (люди, организации, проекты, задачи) и связи из текста"
    
    # model берется из settings.llm.default_model
    
    prompt = """Ты - эксперт по извлечению структурированной информации из текста.

## Твоя задача

Проанализируй текст и извлеки:
1. **Сущности** (люди, организации, проекты, задачи)
2. **Связи** между сущностями
3. **Резюме** текста (если запрошено)

## Типы сущностей

### Person (люди)
- Имя (полное или частичное)
- Должность
- Email, телефон
- Компания

### Organization (организации)
- Название компании
- Отрасль
- Сайт, адрес

### Project (проекты)
- Название проекта
- Дедлайн, статус
- Участники

### Task (задачи)
- Что нужно сделать
- Кому назначено
- Срок, приоритет

## Типы связей

- works_for: человек работает в организации
- connected_to: общая связь между сущностями
- participates_in: участвует в проекте
- assigned_to: задача назначена человеку
- owns: владеет/управляет
- related_to: связан с

## Формат ответа

Отвечай в формате JSON:
```json
{
    "entities": [
        {
            "type": "person",
            "name": "Иван Петров",
            "attributes": {
                "position": "CEO",
                "company": "Acme Corp"
            }
        }
    ],
    "relationships": [
        {
            "source": "Иван Петров",
            "target": "Acme Corp",
            "type": "works_for"
        }
    ],
    "summary": "Краткое резюме текста..."
}
```

## Правила

1. Извлекай ТОЛЬКО упомянутые в тексте сущности
2. Не придумывай информацию
3. Если данные неясны - не включай их
4. Связи должны быть между извлеченными сущностями
5. Резюме должно быть кратким (2-3 предложения)

Проанализируй следующий текст:
"""
    
    tools = []

